image:
  tag: "1.1.0"
  pullPolicy: Always

secrets:
  databaseSecret: neon-tenant-db-secret-prod
  firebaseSecret: firebase-secret-prod

kafka:
  bootstrapServers: "kafka.kafka-prod.svc.cluster.local:9092"

configServer:
  enabled: true

spring:
  profiles:
    active: prod
